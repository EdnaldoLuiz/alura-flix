import './Header.css';

export const Header = (props) => {
    console.log(props);
    return (
        <>
            <header>
                <h1>My React App</h1>
            </header>
        </>
    );
};